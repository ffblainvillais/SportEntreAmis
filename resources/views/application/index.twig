{% extends 'layout/layout' %}

{% block content %}
    {{ parent() }}

    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-default">

                    <div class="panel-body">

                        {% if auth_user() %}
                           Bonjour {{ auth_user().name }}
                        {% endif %}

                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}